This directory should only hold versions of the uploader. Both the python script and compiled exe should be included for any single version
===========================================================================================================================================
